# rabbitmq-nodejs-demo

simple rabbitmq nodejs demo, like hello world but for rabbitmq

# prerequisites

docker & docker compose

# how to run

simply run `docker compose up -d` in project root directory
